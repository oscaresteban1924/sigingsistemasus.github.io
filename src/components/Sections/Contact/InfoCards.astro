---
// src/components/Sections/Contact/InfoCards.astro — Tarjetas de contacto académico.
import type { BadgeVariant } from "@components/ui/badge";
import { Badge } from "@components/ui/badge";
import { PixelPanel } from "@components/ui/pixel-panel";
import siteData from "@config/siteData.json";
import { socialHref } from "@config/socialData.json";

const email = siteData.author.email;
const github = socialHref("github");

const display = (url: string): string => url.replace(/^https?:\/\//, "").replace(/\/$/, "");

const cards: readonly {
  tag: string;
  tone: BadgeVariant;
  title: string;
  blurb: string;
  value: string;
  href: string;
}[] = [
  {
    tag: "Correo Docente",
    tone: "info",
    title: "Atención Estudiantil",
    blurb: "Canal directo con la coordinación del curso",
    value: email,
    href: `mailto:${email}`,
  },
  {
    tag: "Comunidad",
    tone: "success",
    title: "Foro Estudiantil",
    blurb: "Espacio de discusión e inquietudes académicas",
    value: display(github),
    href: github,
  },
  {
    tag: "Código Fuente",
    tone: "secondary",
    title: "GitHub Oficial",
    blurb: "Repositorio con guías y código de laboratorios",
    value: display(github),
    href: github,
  },
];
---

<div class="flex flex-col gap-6">
  {
    cards.map((card) => (
      <PixelPanel class="flex flex-col items-start gap-4 p-6">
        <Badge pixel variant={card.tone}>
          {card.tag}
        </Badge>
        <h2 class="h3 text-foreground">{card.title}</h2>
        <p class="text-muted-foreground text-xs">{card.blurb}</p>
        <a
          href={card.href}
          class="bg-accent border-border text-info hover:text-success primary-focus block w-full truncate border-2 p-3 text-sm uppercase transition-colors"
        >
          {card.value}
        </a>
      </PixelPanel>
    ))
  }
</div>
